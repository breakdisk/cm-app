# Never in the tree. The sibling driver app has its release keystore committed
# in three files under different names; this app takes signing material from CI
# secrets only.
*.jks
*.keystore
*.p12
keystore*.txt
google_services*.txt
maps_api_key*.txt
google-services.json
local.properties

.gradle/
build/
.idea/
*.iml
captures/
