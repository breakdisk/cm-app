package io.logisticos.driver.navigation

import androidx.compose.runtime.Composable

@Composable
fun AppNavGraph() {
    // Navigation graph wired in Task 8 (auth) and Task 22 (bottom nav)
    // Stub to allow MainActivity to compile
}
