<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
        USE_BIOMETRIC removed: nothing in the app calls BiometricPrompt, so it was
        an unused permission declaration. Re-add it alongside the code that needs it.
    -->

    <!--
        RECEIVE_BOOT_COMPLETED is consumed by WorkManager's own boot receiver, which
        reschedules the periodic OutboundSyncWorker after a restart. The app declares
        no boot receiver of its own; the location foreground service is deliberately
        started from the Home screen once location permission is confirmed, because
        startForeground(FOREGROUND_SERVICE_TYPE_LOCATION) throws on Android 14+ if
        the runtime grant isn't already held.
    -->

    <queries>
        <package android:name="com.symbol.datawedge" />
        <package android:name="com.honeywell.aidc" />
    </queries>

    <application
        android:name=".DriverApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.DriverApp"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Driver invite deep link — handles URLs of the form:
                  https://driver.cargomarket.net/join?t=<slug>&p=<phone>&sig=<hmac>
                android:autoVerify="true" enables App Links (verified HTTPS), so
                Android opens the app directly without the disambiguation dialog.
                Requires a /.well-known/assetlinks.json entry on driver.cargomarket.net.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="driver.cargomarket.net"
                    android:pathPrefix="/join" />
            </intent-filter>
        </activity>

        <!-- FCM -->
        <service
            android:name="io.logisticos.driver.feature.notifications.DriverMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Location foreground service -->
        <service
            android:name="io.logisticos.driver.core.location.LocationForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- Google Maps API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!--
            ARCore — "optional" keeps the app installable on non-AR devices.
            Drivers on unsupported hardware fall back to manual dimension entry.
            Change to "required" only if AR measurement becomes mandatory.
        -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />

        <!--
            FileProvider — grants the system camera app temporary write-URI
            access to files inside context.filesDir. Required so TakePicture
            can save POD photos without world-readable external storage.
            Authority matches "${context.packageName}.fileprovider" used in
            PodScreen's FileProvider.getUriForFile() call.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- Disable default WorkManager initializer so Hilt's Configuration.Provider is used -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>
</manifest>
