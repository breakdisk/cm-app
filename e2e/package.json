{
  "name": "@logisticos/e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end Playwright tests for the LogisticOS auth bridge and portal flows.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "typescript": "^5.5.0"
  }
}
