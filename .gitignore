# Rust
target/
# Cargo.lock is intentionally committed for reproducible Docker builds

# Environment
.env
.env.local
.env.*.local

# Node / JS
node_modules/
.next/
.turbo/
dist/
build/
out/
*.tsbuildinfo

# React Native
.expo/
android/
ios/
*.keystore

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Infra / Secrets
infra/terraform/.terraform/
infra/terraform/**/*.tfstate
infra/terraform/**/*.tfstate.backup
infra/terraform/**/*.tfvars
!infra/terraform/**/*.tfvars.example
*.pem
*.key
*.crt

# Logs
*.log
logs/

# Coverage
coverage/
lcov.info

# Docker
docker-compose.override.yml

# Generated
libs/sdk/generated/
libs/proto/src/generated/

# Git worktrees
.worktrees/
.claude/worktrees/
.claude/scheduled_tasks.lock
.claire/
.clone/
.playwright-mcp/
.cargo/

# Android (driver-app-android) — build output and local tooling
apps/driver-app-android/.gradle/
apps/driver-app-android/build/
apps/driver-app-android/**/build/
apps/driver-app-android/local.properties
apps/driver-app-android/gradle/wrapper/gradle-wrapper.jar
# gradlew and gradlew.bat must be committed — CI needs them to build the APK
apps/driver-app-android/gradle/gradle-daemon-jvm.properties

# Firebase / Google service account secrets
google-services.json
**/google-services.json
agenticlogistic-firebase-adminsdk-*.json
*-firebase-adminsdk-*.json
