// Dispatch-specific middleware.
// Currently inherits all middleware from logisticos_auth (require_auth, require_permission).
// Future: dispatch-specific rate limiting per tenant for high-frequency route queries.
