//! External service adapters for the identity service.
//! Currently empty — email verification links will be sent via the engagement service
//! by consuming the `tenant.created` / `user.invited` Kafka events.
//! Future: direct OIDC provider integration (Google, Microsoft SSO) goes here.
